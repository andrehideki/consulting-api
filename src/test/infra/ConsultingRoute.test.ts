import axios from "axios";

describe("ConsultingRoute", () => {
  
  test("Should get consulting information if user category is OWNER", async () => {
    // const resp = await axios.get("http://localhost:3333/consulting?email=fulano@hotmail.com");
  });
  
});